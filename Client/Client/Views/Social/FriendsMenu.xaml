<Window x:Class="Client.Views.Social.FriendsMenu"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Friends" Height="500" Width="850"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        Background="#1E1E1E">
    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="43*"/>
            <ColumnDefinition Width="92*"/>
        </Grid.ColumnDefinitions>
        <TabControl Background="Transparent" BorderThickness="0" Grid.ColumnSpan="2">

            <TabItem Header="My Friends" FontSize="16" Foreground="Black">
                <Grid Margin="0,10,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <DataGrid x:Name="DataGridFriends" Grid.Row="0" AutoGenerateColumns="False" IsReadOnly="True" 
                              Background="White" Margin="0,0,0,10">
                        <DataGrid.Columns>
                            <DataGridTemplateColumn Header="Avatar" Width="60">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Image Source="{Binding AvatarImage}" Width="40" Height="40" Stretch="UniformToFill"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <DataGridTextColumn Header="Username" Binding="{Binding Username}" Width="*"/>

                            <DataGridTemplateColumn Header="Status" Width="80">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding StatusText}" Foreground="{Binding StatusColor}" FontWeight="Bold" VerticalAlignment="Center"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <DataGridTemplateColumn Width="100">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Content="Remove" Click="ButtonRemoveFriend_Click" Tag="{Binding Username}" Background="#FF5555" Foreground="White"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>

                    <Button x:Name="ButtonBackFriends" Grid.Row="1" Content="Back" Click="ButtonBack_Click" Width="100" HorizontalAlignment="Left" Height="30"/>
                </Grid>
            </TabItem>

            <TabItem Header="Requests &amp; Search" FontSize="16" Foreground="Black">
                <Grid Margin="0,10,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <GroupBox Header="Add Friend" Grid.Column="0" Margin="0,0,10,0" Foreground="White" BorderBrush="Gray">
                        <StackPanel VerticalAlignment="Center" Margin="20">
                            <Label Content="Enter Username:" Foreground="White"/>
                            <TextBox x:Name="TextBoxSearchUser" Height="30" Margin="0,5"/>
                            <Button x:Name="ButtonSendRequest" Content="Send Request" Click="ButtonSendRequest_Click" Height="35" Margin="0,15"/>
                        </StackPanel>
                    </GroupBox>

                    <GroupBox Header="Pending Requests" Grid.Column="1" Margin="10,0,0,0" Foreground="White" BorderBrush="Gray">
                        <ListView x:Name="ListViewRequests" Background="White" Foreground="Black">
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="5">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <TextBlock Grid.Column="1" Text="{Binding SenderUsername}" VerticalAlignment="Center" FontWeight="Bold" Margin="5,0"/>

                                        <Button Grid.Column="2" Content="Accept" Click="ButtonAcceptRequest_Click" Tag="{Binding RequestId}" 
                                                Margin="5,0" Background="#55FF55" Width="60"/>

                                        <Button Grid.Column="3" Content="Decline" Click="ButtonDeclineRequest_Click" Tag="{Binding RequestId}" 
                                                Background="#FF5555" Width="60" Foreground="White"/>
                                    </Grid>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </GroupBox>

                    <Button x:Name="ButtonBackRequests" Content="Back" Click="ButtonBack_Click" 
                            Grid.Column="0" VerticalAlignment="Bottom" HorizontalAlignment="Left" Width="100" Height="30" Margin="0,10,0,-40"/>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</Window>