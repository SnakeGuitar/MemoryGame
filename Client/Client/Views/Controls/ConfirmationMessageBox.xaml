<Window x:Class="Client.Views.Controls.ConfirmationMessageBox"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Client.Views.Controls"
        mc:Ignorable="d"
        xmlns:language="clr-namespace:Client.Properties.Langs"
        
        SizeToContent="WidthAndHeight"
        MinWidth="550" MaxWidth="700"
        WindowStartupLocation="CenterOwner"
        
        AllowsTransparency="True"
        WindowStyle="None"
        Background="Transparent"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">

    <Border CornerRadius="15" 
        Background="{StaticResource BackgroundColor}"
        BorderBrush="{StaticResource AccentColor}"
        BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect ShadowDepth="0" BlurRadius="10" Opacity="0.3" Color="Black"/>
        </Border.Effect>

        <Grid>
            <Grid Margin="20,10,20,20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock x:Name="TextBlockTitle" Text="Title"
                       Grid.Row="0" Margin="0,10,0,0"
                       Style="{StaticResource TitleTextStyle}"
                       TextWrapping="Wrap" TextAlignment="Center" />

                <Border x:Name="MessageBorder" Grid.Row="1"
                    Background="{StaticResource AccentColor}" 
                    CornerRadius="10" Margin="0,15,0,15">

                    <TextBlock x:Name="TextBlockMessage" Text="Message"
                           Style="{StaticResource MessageBoxTextStyle}"/>
                </Border>

                <Button Content="{x:Static language:Lang.Global_Button_Accept}" 
                        Grid.Row="2" Style="{StaticResource SecondaryButtonStyle}"
                        Margin="55,0,280,20"
                        Click="ButtonAccept_Click"/>

                <Button Content="{x:Static language:Lang.Global_Button_Cancel}"
                        Grid.Row="2" Style="{StaticResource SecondaryButtonStyle}"
                        Margin="220,0,0,20"
                        Click="ButtonCancel_Click"/>
            </Grid>
        </Grid>
    </Border>
</Window>
