<Window x:Class="Client.Views.Profile.EditProfile"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Editar Perfil" Height="688" Width="450"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        Background="{StaticResource BackgroundColor}">

    <Grid Margin="20">
        <StackPanel>
            <Label Content="Editar Perfil" FontSize="24" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center" Margin="0,0,0,20"/>

            <GroupBox Header="Avatar" Foreground="White" Margin="0,0,0,15">
                <StackPanel Orientation="Horizontal" Margin="10">
                    <Border BorderBrush="Gray" BorderThickness="1" CornerRadius="5" Margin="0,0,15,0">
                        <Image x:Name="ImageAvatar" Width="80" Height="80" Stretch="UniformToFill"/>
                    </Border>
                    <Button x:Name="ButtonChangeAvatar" Content="Cambiar Avatar" Click="ButtonChangeAvatar_Click" VerticalAlignment="Center" Height="30" Width="120"/>
                </StackPanel>
            </GroupBox>

            <GroupBox Header="Cambiar Contraseña" Foreground="White" Margin="0,0,0,15">
                <StackPanel Margin="10">
                    <Label Content="Contraseña Actual:" Foreground="White"/>
                    <PasswordBox x:Name="PasswordBoxCurrent" Height="30" Margin="0,0,0,5"/>

                    <Label Content="Nueva Contraseña:" Foreground="White"/>
                    <PasswordBox x:Name="PasswordBoxNew" Height="30"/>

                    <Button x:Name="ButtonUpdatePassword" Content="Actualizar Contraseña" Click="ButtonUpdatePassword_Click" Margin="0,15,0,0" Height="30"/>
                </StackPanel>
            </GroupBox>

            <GroupBox Header="Cambiar Nombre de Usuario" Foreground="White" Margin="0,0,0,15">
                <StackPanel Margin="10">
                    <Label Content="Nuevo Nombre de Usuario:" Foreground="White"/>
                    <TextBox x:Name="TextBoxNewUsername" Height="30"/>
                    <Button x:Name="ButtonUpdateUsername" Content="Actualizar Nombre" Click="ButtonUpdateUsername_Click" Margin="0,15,0,0" Height="30"/>
                </StackPanel>
            </GroupBox>

            <Button x:Name="ButtonBack" Content="Volver" Click="ButtonBack_Click" Margin="0,10,0,0" Height="35" Width="100" HorizontalAlignment="Center"/>
        </StackPanel>
    </Grid>
</Window>