<Window x:Class="Client.Views.Session.VerifyCode"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Client.Views.Session"
        mc:Ignorable="d"
        xmlns:language="clr-namespace:Client.Properties.Langs"
        
        Title="{x:Static language:Lang.Global_Button_Verify}" 
        MinHeight="600" MinWidth="500" 
        WindowStartupLocation="CenterScreen">

    <Grid Background="{StaticResource BackgroundColor}">

        <Grid HorizontalAlignment="Center" 
              VerticalAlignment="Center">

            <Viewbox Stretch="Uniform"
                     MaxWidth="1200" MaxHeight="1400">

                <StackPanel HorizontalAlignment="Center" 
                            VerticalAlignment="Center"
                            Width="500" Margin="0,0,0,50">

                    <Label Content="{x:Static language:Lang.Global_Button_SignIn}" 
                           Style="{StaticResource TitleLabelStyle}" />

                    <Border Background="{StaticResource AccentColor}" 
                            CornerRadius="15">

                        <StackPanel Margin="25" HorizontalAlignment="Center" VerticalAlignment="Center">

                            <Label Content="{x:Static language:Lang.VerifyCode_Label_SentTo}" 
                                   Style="{StaticResource RegisterLabelStyle}"/>

                            <Label x:Name="LabelRegisterEmail" Content="example@gmail.com" 
                                   Style="{StaticResource VerifyEmailLabelStyle}"/>

                            <Label Content="{x:Static language:Lang.Global_Label_EnterCode}" 
                                   Style="{StaticResource RegisterLabelStyle}"/>

                            <TextBox x:Name="TextBoxInputVericationCode" 
                                     Style="{StaticResource CodeTextBoxStyle}"
                                     PreviewTextInput="NumericOnly_PreviewTextInput"
                                     PreviewKeyDown="TextBox_PreviewKeyDown"/>

                            <Label x:Name="LabelCodeError" 
                                   Style="{StaticResource RegisterErrorLabelStyle}"
                                   Content=""/>

                            <Button Content="{x:Static language:Lang.Global_Button_Verify}" 
                                    Style="{StaticResource FormPrimaryButtonStyle}"
                                    HorizontalAlignment="Center" 
                                    Margin="0,0,0,0"
                                    Name="ButtonVerifyCode"
                                    Click="ButtonVerifyCode_Click" Width="249" />
                        </StackPanel>
                    </Border>

                    <Button Content="{x:Static language:Lang.VerifyCode_Button_SentAgain}" 
                            Style="{StaticResource HyperlinkButtonStyle}"
                            HorizontalAlignment="Center" 
                            Margin="0,20,0,0"
                            Name="ButtonResendCode"
                            Click="ButtonResendVerificationCode_Click" />

                    <Button Content="{x:Static language:Lang.Global_Button_Back}" 
                            Style="{StaticResource SecondaryButtonStyle}"
                            HorizontalAlignment="Center" 
                            Margin="0,15,0,0"
                            Click="ButtonBackToSignIn_Click" />
                </StackPanel>
            </Viewbox>
        </Grid>
    </Grid>
</Window>