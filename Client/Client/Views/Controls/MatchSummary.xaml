<Window x:Class="Client.Views.Controls.MatchSummary"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        xmlns:language="clr-namespace:Client.Properties.Langs"
        
        SizeToContent="WidthAndHeight"
        MinWidth="500" MaxWidth="700"
        WindowStartupLocation="CenterScreen"
        
        AllowsTransparency="True"
        WindowStyle="None"
        Background="Transparent"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">

    <Border CornerRadius="15" 
            Background="{StaticResource BackgroundColor}"
            BorderBrush="{StaticResource AccentColor}"
            BorderThickness="1">

        <Border.Effect>
            <DropShadowEffect ShadowDepth="0" BlurRadius="10" Opacity="0.3" Color="Black"/>
        </Border.Effect>

        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Text="{x:Static language:Lang.Global_Title_MatchOver}"
                       Grid.Row="0" FontSize="50"
                       Style="{StaticResource TitleTextStyle}"
                       Margin="0,10,0,15"
                       Foreground="{StaticResource PrimaryTextColor}"
                       TextWrapping="Wrap" 
                       TextAlignment="Center"/>

            <Border Grid.Row="1"
                    Background="{StaticResource GreenGame}" 
                    CornerRadius="10" 
                    Margin="0,0,0,20"
                    Padding="20">

                <StackPanel HorizontalAlignment="Center">
                    <TextBlock x:Name="TextBlockWinner" 
                               Text="Winner" 
                               Style="{StaticResource MessageBoxTextStyle}"
                               FontSize="40" FontWeight="Bold" Foreground="{StaticResource NormalColor}"
                               Margin="0,0,0,10"/>

                    <Label x:Name="LabelSubtitle"
                           Content="{x:Static language:Lang.MatchSummary_Label_Summary}"
                           Style="{StaticResource RegisterLabelStyle}" Foreground="{StaticResource BackgroundColor}"/>

                    <TextBlock x:Name="TextBlockScore" 
                               Text="Puntaje: 100" 
                               Style="{StaticResource MessageBoxTextStyle}"
                               FontSize="28"/>
                </StackPanel>
            </Border>

            <Button Content="{x:Static language:Lang.Global_Button_Accept}" 
                    Grid.Row="2" 
                    Style="{StaticResource SecondaryButtonStyle}"
                    HorizontalAlignment="Center"
                    Width="150"
                    Click="ButtonAccept_Click"/>
        </Grid>
    </Border>
</Window>