<Window x:Class="Client.Views.Profile.StatsHistory"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Client.Views.Profile"
        xmlns:converters="clr-namespace:Client.Utilities.Converters"
        
        mc:Ignorable="d"
        xmlns:language="clr-namespace:Client.Properties.Langs"
        
        Title="Match History" 
        MinHeight="720" MinWidth="1280"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <converters:SelfReportVisibilityConverter x:Key="SelfReportVisiblityConverter"/>
    </Window.Resources>

    <Grid Background="{StaticResource BackgroundColor}">

        <Viewbox Stretch="Uniform">

            <Grid Width="1450" Height="900">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Label Grid.Row="0" 
                       Content="{x:Static language:Lang.StatsHistory_Title_StatsHistory}" 
                       Style="{StaticResource TitleLabelStyle}"
                       Margin="0,30,0,20"/>

                <Border Grid.Row="1" 
                        Background="{StaticResource PrimaryTextColor}" 
                        CornerRadius="20" 
                        BorderBrush="{StaticResource AccentColor}" 
                        BorderThickness="4"
                        Margin="100,10,100,30"
                        Padding="20">

                    <Grid>
                        <DataGrid x:Name="DataGridHistory" 
                                  AutoGenerateColumns="False" 
                                  IsReadOnly="True" 
                                  Background="Transparent"
                                  BorderThickness="0"
                                  HeadersVisibility="Column"
                                  GridLinesVisibility="Horizontal"
                                  RowBackground="Transparent"
                                  AlternatingRowBackground="#10000000"
                                  HorizontalScrollBarVisibility="Disabled"
                                  VerticalScrollBarVisibility="Auto">

                            <DataGrid.Resources>
                                <Style TargetType="DataGridColumnHeader">
                                    <Setter Property="Background" Value="Transparent"/>
                                    <Setter Property="FontFamily" Value="{StaticResource BodyFont}"/>
                                    <Setter Property="FontSize" Value="30"/>
                                    <Setter Property="FontWeight" Value="Bold"/>
                                    <Setter Property="Foreground" Value="{StaticResource AccentForegroundColor}"/>
                                    <Setter Property="Padding" Value="10,15"/>
                                    <Setter Property="HorizontalContentAlignment" Value="Center"/>
                                </Style>

                                <Style TargetType="DataGridCell">
                                    <Setter Property="Padding" Value="10"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="DataGridCell">
                                                <Border Padding="{TemplateBinding Padding}" Background="{TemplateBinding Background}">
                                                    <ContentPresenter VerticalAlignment="Center" />
                                                </Border>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </DataGrid.Resources>

                            <DataGrid.Columns>
                                <DataGridTextColumn Header="{x:Static language:Lang.StatsHistory_Label_Date}" 
                                                    Binding="{Binding Date, StringFormat=d}" 
                                                    Width="*">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="FontFamily" Value="{StaticResource BodyFont}"/>
                                            <Setter Property="FontSize" Value="24"/>
                                            <Setter Property="Foreground" Value="{StaticResource AccentForegroundColor}"/>
                                            <Setter Property="HorizontalAlignment" Value="Center"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>

                                <DataGridTextColumn Header="{x:Static language:Lang.StatsHistory_Label_Winner}" 
                                                    Binding="{Binding WinnerName}" 
                                                    Width="*">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="FontFamily" Value="{StaticResource BodyFont}"/>
                                            <Setter Property="FontSize" Value="24"/>
                                            <Setter Property="FontWeight" Value="Bold"/>
                                            <Setter Property="Foreground" Value="{StaticResource NormalColor}"/>
                                            <Setter Property="HorizontalAlignment" Value="Center"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>

                                <DataGridTextColumn Header="{x:Static language:Lang.StatsHistory_Label_Score}" 
                                                    Binding="{Binding Score}" 
                                                    Width="200">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="FontFamily" Value="{StaticResource BodyFont}"/>
                                            <Setter Property="FontSize" Value="24"/>
                                            <Setter Property="Foreground" Value="{StaticResource AccentForegroundColor}"/>
                                            <Setter Property="HorizontalAlignment" Value="Center"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>

                                <DataGridTemplateColumn Width="150">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Button Content="{x:Static language:Lang.Global_Button_Report}"
                                                    Tag="{Binding}" 
                                                    Style="{StaticResource HardButtonStyle}"
                                                    Foreground="White" FontSize="18"
                                                    Height="40" Width="120"
                                                    Click="ButtonReport_Click"
                                                    
                                                    Visibility="{Binding WinnerName, Converter={StaticResource SelfReportVisiblityConverter}}"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Grid>
                </Border>

                <Button Grid.Row="2" 
                        Content="{x:Static language:Lang.Global_Button_Back}" 
                        Style="{StaticResource SecondaryButtonStyle}"
                        HorizontalAlignment="Center"
                        Width="200" Height="60" FontSize="24"
                        Margin="0,0,0,40"
                        Click="ButtonBack_Click"/>
            </Grid>
        </Viewbox>
    </Grid>
</Window>